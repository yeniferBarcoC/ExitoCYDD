using System.Runtime.Serialization;
using System.Runtime.InteropServices.ComTypes;
using System.Data;
using System;

namespace Consola
{
    class Program
    {
        static void Main(string[] args)
        {
            /*Dominio em = new Dominio.Empleado();
            em.Nombre = "Johan";
            Conexion conn = new Conexion();
            ConnectionState.Empleado.Add(em);
            conn.SaveChanges();  */
        }
    }
}
